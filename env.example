# Convert this to an .env file and update the values before executing setup-argocd.sh - also, be sure to never commit secrets to git!

# IMPORTANT: Replace "your-argocd-machine-username" with your Argo CD machine username,
# and replace "ghp_yourGitHubPersonalAccessToken" with an actual GitHub personal 
# access token or deploy key *before* running ./setup-argocd.sh !  
#
# For detailed instructions on how to create a machine user and GitHub token,
# see: https://www.essesseff.com/docs/deployment/github-argocd-machine-user-setup 
#
# SECURITY: BE SURE TO DELETE OR EXCLUDE THIS FILE FROM GIT TO PREVENT
# COMMITTING YOUR GITHUB TOKEN(S) TO GITHUB!
#
# Required Permissions for GitHub Token:
#   - repo (Full control of private repositories)
#   - read:packages (Download packages from GitHub Container Registry)

# GitHub Machine User Credentials
ARGOCD_MACHINE_USER="your-argocd-machine-username"
GITHUB_TOKEN="ghp_yourGitHubPersonalAccessToken"
ARGOCD_MACHINE_EMAIL="your-email@example.com"

# Organization/App Config (optional - these are also already set in setup-argocd.sh)
GITHUB_ORG="essesseff-helloworld-springboot-templat"
APP_NAME="helloworld"
ENVIRONMENT="staging"
